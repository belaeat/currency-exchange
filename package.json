{
  "name": "exchange-rate",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && cp src/index.html dist/ && cp src/style.css dist/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^5.0.8"
  }
}
