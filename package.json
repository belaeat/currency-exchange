{
  "name": "exchange-rate",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc && vite build",
    "predeploy": "npm run build"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^5.0.8"
  }
}
